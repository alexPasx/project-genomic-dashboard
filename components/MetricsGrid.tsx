import { Activity, Dna, Ruler, Sigma } from "lucide-react";
import { formatInteger, type GeneData } from "../data/mockData";
export function MetricsGrid({ gene, total, filtering }: { gene: GeneData; total: number; filtering: boolean }) { const cards = [{label:"Total variants",value:formatInteger(total),note:filtering?"Filtered subset":"Across selected cohort",icon:Dna,accent:"violet"},{label:"Mean allele frequency",value:gene.meanAF.toFixed(5),note:"Population-weighted AF",icon:Activity,accent:"blue"},{label:"Gene constraint",value:gene.pli.toFixed(2),note:`pLI · LOEUF ${gene.loeuf.toFixed(2)}`,icon:Sigma,accent:"mint"},{label:"Genomic span",value:`${(gene.length/1000).toFixed(1)} kb`,note:`${gene.exons} coding exons`,icon:Ruler,accent:"pink"}]; return <section className="metrics-grid">{cards.map(({label,value,note,icon:Icon,accent})=><article className={`metric-card ${accent}`} key={label}><div className="metric-icon"><Icon size={18}/></div><div><p>{label}</p><strong>{value}</strong><span>{note}</span></div></article>)}</section>; }
