"use client";
import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from "recharts";
import { ChartCard } from "./ChartCard";
import { categoryColors, categoryLabels, formatInteger, type CategoryKey, type GeneData } from "../data/mockData";
export function DonutCard({ gene, active }: { gene: GeneData; active: CategoryKey[] }) { const data=active.map(key=>({key,name:categoryLabels[key],value:gene.consequences[key]})); const total=data.reduce((s,d)=>s+d.value,0); return <ChartCard title="Variant consequences" subtitle="Distribution by predicted functional effect" tag="OVERVIEW"><div className="donut-layout"><div className="donut-chart"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" innerRadius="66%" outerRadius="90%" paddingAngle={3} stroke="none">{data.map(d=><Cell key={d.key} fill={categoryColors[d.key]}/>)}</Pie><Tooltip contentStyle={{background:"#171a26",border:"1px solid #303547",borderRadius:10}} itemStyle={{color:"#f5f7ff"}}/></PieChart></ResponsiveContainer><div className="donut-center"><strong>{formatInteger(total)}</strong><span>variants</span></div></div><div className="legend-list">{data.map(d=><div className="legend-row" key={d.key}><span className="swatch" style={{background:categoryColors[d.key]}}/><span>{d.name}</span><strong>{Math.round(d.value/total*100)}%</strong></div>)}</div></div></ChartCard>; }
