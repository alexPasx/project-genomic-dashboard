"use client";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { ChartCard } from "./ChartCard";
import type { CategoryKey, GeneData } from "../data/mockData";
export function GeneCoverageCard({ gene, active }: { gene: GeneData; active: CategoryKey[] }) { const ratio=active.reduce((s,k)=>s+gene.consequences[k],0)/gene.totalVariants; const data=gene.coverage.map(d=>({...d,density:Math.max(2,Math.round(d.density*ratio))})); return <ChartCard title="Gene coordinate density" subtitle="Variant density and sequencing depth across exons" tag="GRCh38" className="wide-card"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{top:12,right:8,left:-22,bottom:0}}><defs><linearGradient id="densityFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#9b6cff" stopOpacity=".62"/><stop offset="100%" stopColor="#9b6cff" stopOpacity="0"/></linearGradient></defs><CartesianGrid stroke="#272b3a" vertical={false} strokeDasharray="3 5"/><XAxis dataKey="position" axisLine={false} tickLine={false} tick={{fill:"#747b91",fontSize:10}} tickFormatter={v=>`${Math.round(v/1000)}k`}/><YAxis axisLine={false} tickLine={false} tick={{fill:"#747b91",fontSize:10}}/><Tooltip labelFormatter={v=>`${Number(v).toLocaleString()} bp`} contentStyle={{background:"#171a26",border:"1px solid #303547",borderRadius:10}}/><Area type="monotone" dataKey="depth" stroke="#4f8cff" strokeWidth={2} fill="none" dot={false} name="Depth"/><Area type="monotone" dataKey="density" stroke="#a56cff" strokeWidth={2} fill="url(#densityFill)" dot={false} name="Variant density"/></AreaChart></ResponsiveContainer></ChartCard>; }
