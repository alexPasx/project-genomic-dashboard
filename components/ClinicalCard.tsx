"use client";
import { Bar, BarChart, CartesianGrid, Legend, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { ChartCard } from "./ChartCard";
import type { CategoryKey, GeneData } from "../data/mockData";
export function ClinicalCard({ gene, active }: { gene: GeneData; active: CategoryKey[] }) { const data=gene.clinical.map(d=>{const factor=active.reduce((s,k)=>s+d.weight[k],0)/4;return{domain:d.domain,pathogenic:-Math.round(d.pathogenic*factor),benign:Math.round(d.benign*factor)}}); return <ChartCard title="Clinical significance" subtitle="ClinVar evidence by functional protein domain" tag="CLINVAR" className="wide-card"><ResponsiveContainer width="100%" height="100%"><BarChart layout="vertical" data={data} margin={{top:6,right:12,left:0,bottom:0}}><CartesianGrid stroke="#272b3a" horizontal={false} strokeDasharray="3 5"/><XAxis type="number" domain={[-60,60]} axisLine={false} tickLine={false} tick={{fill:"#747b91",fontSize:10}} tickFormatter={v=>Math.abs(v).toString()}/><YAxis type="category" dataKey="domain" width={68} axisLine={false} tickLine={false} tick={{fill:"#aeb3c4",fontSize:10}}/><ReferenceLine x={0} stroke="#50566b"/><Tooltip formatter={value=>Math.abs(Number(value))} contentStyle={{background:"#171a26",border:"1px solid #303547",borderRadius:10}}/><Legend wrapperStyle={{fontSize:11,color:"#9ba1b4"}}/><Bar dataKey="pathogenic" name="Pathogenic" fill="#ff5a79" radius={[4,0,0,4]}/><Bar dataKey="benign" name="Benign" fill="#27d6b0" radius={[0,4,4,0]}/></BarChart></ResponsiveContainer></ChartCard>; }
